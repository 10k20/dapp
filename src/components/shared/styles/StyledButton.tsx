import { Button } from '@mui/material';
import { styled } from '@material-ui/core';

export const StyledButton = styled(Button)({
  width: '8.75rem',
  backgroundColor: 'transparent',
  boxShadow: '0px 0px 5px 2px #CB23E780',
  color: '#fff !important'
})